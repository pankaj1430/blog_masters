import 'package:flutter/material.dart';

class PickTopics extends StatelessWidget {
  const PickTopics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
children: [
  

],
      ),
    );
  }
}
